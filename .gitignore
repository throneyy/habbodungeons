# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# agent scratch + large one-off handoff bundle (12MB) — never commit
.gg/
habbo-dungeons-supabase-handoff.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# e2e browser profiles (reused Supabase anon sessions — see partyInviteError.e2e.mjs)
tests/e2e/.profiles/

# e2e screenshots (duelLive playtest)
tests/e2e/.artifacts/

# e2e screenshots (coopFallen visual verification) — regenerated every run
tests/e2e/screenshots/

# Habbo furnidata cache (~9.6MB) pulled by the furni asset pipeline.
# Regenerable from https://www.habbo.com/gamedata/furnidata_json/1 — never committed.
tools/swf/

# Ripped Habbo source art (~4.6MB, 2252 files) — NEVER COMMIT.
# Contains hh_human_item.swf pulled from the live gordon build path, the 2191
# hand-item sprites extracted from it, and archived habbo.com assets (2006
# stylesheets, box-chrome GIFs, navi icons) scraped from the Wayback Machine.
# All of it is Sulake's copyright, kept locally as reference only.
# Fully regenerable: tools/extract-handitems.py re-downloads the SWF and
# re-extracts every sprite; tools/reference/habbo-2006/fetch-assets.sh re-pulls
# the archived web assets.
# NOTE: the 8 baked runtime icons in public/assets/ui/weapons/ stay TRACKED —
# those are shipped product art, not raw rips.
#
# The SCRIPTS that do the re-pulling are tracked too, and so is the manifest of
# what they should have pulled. "Fully regenerable" above was false while the
# regenerating script was itself ignored: a fresh clone had no fetch-assets.sh
# and therefore no way to rebuild anything. Our own tooling and plain-text
# records are not Sulake's copyright; the art they download still is, and
# assets/ plus the scraped index.html stay ignored below.
tools/reference/*
!tools/reference/habbo-2006/
tools/reference/habbo-2006/*
!tools/reference/habbo-2006/*.sh
!tools/reference/habbo-2006/*.md
!tools/reference/habbo-2006/assets-manifest.tsv

# Python bytecode from the tools/ scripts
__pycache__/
*.pyc
